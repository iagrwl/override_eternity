#pragma once

// declare all op control functions written inside "drivetrainControls.cpp"
// so that they can be referenced in different files.

//drive
void handleArcade();
void handleTank();
void handleDriveMode(bool driveMode);
void handleDriveCommands();

